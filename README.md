The goal of this project is to create a custom wearable that can gather data and interpret from that data the hear rate, step count, etc. 
I want it to do this locally on the micro controller (edge computing). This project will broadcast this data (via Bluetooth) into an app or
a UX of some kind to make the data easily readable for users.
